# تصميم «سكينة» على Figma

## الملفات

| الملف | المفتاح | المحتوى |
| --- | --- | --- |
| سكينة — الشريط الموحّد لقارئ المصحف | `iRwBPUnDBLhHEfViQYloDD` | الشريط العلوي الموحّد: الرموز، المكوّنات، الشاشات، الحالات |

الرابط: https://www.figma.com/design/iRwBPUnDBLhHEfViQYloDD

### صفحات الملف

1. **الرموز والمكوّنات** — اثنتا عشرة أيقونة (٢٤×٢٤، مسارات موحّدة بسماكة ١٫٧)، ومجموعة متغيّرات
   `شريط/الشريط الموحّد` بثلاث حالات: `عادي`، `سحب`، `حفظ`.
2. **الشريط الموحّد** — أربع شاشات ٣٩٣×٨٥٢: ورق كريمي، ورق داكن، بلا شريط، والتصميم الحالي للمقارنة.
3. **الحالات والسلوك** — سحب الخطّ الذهبي، جلسة الحفظ، قائمة زرّ «المزيد»، وإطار «أسوأ حالة».
4. **الرئيسية المدمجة (مسودّة أولى)** — نسختان مقارنتان: قبلة كاملة أعلى الشاشة، وقبلة مضغوطة.
5. **الرئيسية — التصميم النهائي** — شاشة واحدة ٣٩٣×٨٥٢ تدمج القبلة والصلاة: بطاقة «الآن» (تدرّج
   أخضر عميق بنقش نجمة ثمانية، بوصلة ميدالية ١٤٠ بعلامات درجات وإبرة ذهبية متدرّجة، اسم الصلاة
   القادمة بخطّ Reem Kufi ٤٠ والعدّاد Readex Light ٤٠، شريط تقدّم ذهبي)، ثم بطاقة مواقيت اليوم
   (ستّة صفوف برموز الشمس والقمر وأجراس التذكير وصفّ «القادمة» مميّز)، وشريط تبويبات عائم رباعي:
   الرئيسية · المصحف · الأذكار · المزيد. الرموز كلّها مسارات SVG بسماكة ١٫٧٥ ورؤوس مستديرة.
6. **الرئيسية — الحيويّة (v6)** — صفحة `٦ · الرئيسية — الحيويّة (v6)` (node `37:44`): إعادة تصميم كاملة
   بثلاثة إطارات ولوحة دليل. الصفحة كاملة (`37:45`، ٣٩٣×١٥٦١) والشاشة الأولى مساءً (`43:44`)
   وحالة السَّحَر (`44:44`) ودليل التصميم (`45:44`). الفكرة: هيرو سماءٍ يتبدّل مع وقت الصلاة
   (تدرّج ليل→زمرّد مع شفق المغرب، أو نيلي→وردي→كهرمان قبل الفجر) بنقش نجوم ثمانية؛ العدّ التنازلي
   Readex Light ٦٠ هو اللحظة الكبرى، واسم الصلاة Reem Kufi Bold ٥٤، وميدالية القبلة ١٣٢ إلى جانبه
   بحلقة نبض نعناعية وحبّة «متّجه نحو القبلة»، ثم **خطّ اليوم**: مسار بست عقد للصلوات، المنقضي ذهبي،
   وعلامة «الآن» (هلال ليلًا/شمس سَحَرًا) تزحف عليه. تحت الهيرو: أربع بلاطات وصول سريع عائمة على
   حافته المنحنية (المصحف ص ٢٩٦ · الأذكار المساء · القبلة ١٥١° · المساجد ٤٥٠ م)، بطاقة مواقيت اليوم
   كشبكة ٣×٢ والقادمة معبّأة بلون العلامة، بطاقة متابعة القراءة بصورة صفحة مصغّرة وتقدّم الختمة،
   بطاقة أقرب مسجد بشريحة خريطة (مسار سير منقّط ودبّوس)، وبطاقة أذكار المساء الزمرّدية بحلقة تقدّم.
   الشريط العائم كما في التطبيق. اللوحة والطباعة والحركة موثّقة في لوحة الدليل داخل الصفحة.
7. **نظام السماء والطقس** — صفحة `٧ · نظام السماء والطقس` (node `49:44`): السماء في الرئيسية تتبدّل
   مع الوقت وتتأثّر بالطقس، ويختار المستخدم بديلًا في الإعدادات. **تسعة أطوار** (`49:45`) تُشتقّ من
   مواقيت الصلاة نفسها بلا مصدر إضافي: الليل (بعد العشاء → ثلث الليل الأخير)، السَّحَر، الفجر، الشروق
   (+ساعة)، الضحى، الظهر، العصر، الغروب (−٤٠ د من المغرب)، الشفق (المغرب → العشاء، التصميم الحالي).
   لكل طور تدرّج ثلاثي/رباعي وقرص شمس/قمر ووهج أفق و**وضع نصّ** (ورق على الداكن، حبر على الفاتح)،
   والانتقال تدرّج على ٢٠ دقيقة حول كل حدّ. **الطقس طبقة** (`51:44`) لا لوحة: صافٍ، غيوم متفرّقة، غائم
   (−٢٥٪ تشبّعًا −١٥٪ إضاءة)، مطر (خطوط مائلة)، غبار (طبقة رملية — مهمّ للخليج)، ثلج؛ تُشتقّ من رمز
   WMO والغطاء السحابي عبر Open-Meteo (بلا مفتاح ولا حساب، إحداثيات مقرّبة ~١ كم، مرّة كل ساعة،
   اختياري من الإعدادات). **شاشة الإعدادات «مظهر السماء»** (`52:44`): معاينة حيّة، وضع تلقائي/ثابت/مخصّص،
   مفتاح الطقس مع ملاحظة الخصوصية، تقليل الحركة، اختيار طور ثابت من تسعة، أو لون مخصّص من ثمانية
   (يلوّن السماء والشريط والبطاقات المعبّأة؛ الذهب والنعناع ثابتان للإشارات).

   **التنفيذ (5.1.0 — الصفحتان ٦ و٧):** المنطق المشترك في النواتين: `SakinahCore/Sky.swift`
   و`core/.../Sky.kt` (`SkyPhase` التسعة من مواقيت اليوم مع مزج ٢٠ دقيقة حول كل حدّ، لوحات الأطوار
   بأربع درجات + الوهج + القرص + وضع النصّ، طبقات الطقس كتعديلات على اللوحة، `SkyWeather.from(wmo:)`
   من رموز WMO والغطاء السحابي والغبار، `SkyPrefs` تلقائي/ثابت/مخصّص، واختبارات وحدة متطابقة في
   `SkyTests.swift` و`SkyTest.kt`). iOS: `App/Sakinah/HomeView.swift` (`HomeView`، `DayTimelineStrip`،
   `MiniPageThumb`، `MosqueMapStrip` بخرائط آبل، `CompassMedallion`، `StarLattice`)،
   `SkyKit.swift` (`SkyWeatherService` من Open-Meteo مع كاش ساعة، `SkyBackdrop` بالتدرّج والنجوم
   والوهج والقرص وطبقات الغيوم/المطر/الثلج/الغبار)، `SkySettingsView.swift` («مظهر السماء»)،
   والتفضيلات في `Settings.sky`. أندرويد: `ui/HomeScreen.kt` (`HomeScreen`، `Hero`،
   `DayTimelineStrip`، `QuickTiles`، البطاقات، `CompassMedallion`)، `app/Sky.kt`
   (`SkyWeatherService`، `SkyBackdrop`)، `ui/SkySettingsScreen.kt`، والتفضيلات في `Store.sky*`.
   شريط `DSTabBar` العائم في `Components.swift` / `ui/Components.kt`. القبلة الكاملة هي شاشة
   القبلة السابقة بزرّ إغلاق. مسارات اللقطات: `qibla` يفتحها فوق الرئيسية، و`home-bottom` يمرّر إلى
   آخر الرئيسية، ووسيطا `-sakinahSky <طور>` و`-sakinahWeather <طقس>` (أندرويد: `--es`) يفرضان
   حالة سماء بعينها للتحقّق البصري من كل طور وطقس في CI (لقطات ٠٧–١٤).

8. **المصحف — إعادة التصميم** — صفحة `٨ · المصحف — إعادة التصميم` (node `55:44`): عشرة إطارات
   ودليل. **المكتبة** (`56:44`، ٣٩٣×١٥٠٠): بطاقة حالةٍ واحدة (صورة الصفحة المصغّرة بشريط علامة + حلقة
   الختمة ٤٤٪ + «ورد اليوم ٤ من ٦» + زرّ «تابع القراءة») بدل ثلاث بطاقات كانت تكرّر النسبة والورد؛
   صفّ وصول لا يكرّر الشريط السفلي (الاستماع · مراجعة الحفظ · العلامات · خطة الختمة)؛ بطاقة «رحلتك
   في القراءة» بخريطة ٩٠ يومًا وسلسلة الأيام؛ فهرس ببحث ومقسّم رباعي (السور · الأجزاء · الأحزاب ·
   العلامات) وأرقام السور في نجمة ثمانية؛ والشريط العائم نفسه من صفحة ٦ (المصحف نشط).
   **القارئ** (`56:46` الصفحة وحدها بالخطّ الذهبي، `56:48` الشريط الموحّد + آية مركّزة بغسلة ذهبية على
   سطورها الفعلية + شريط عائم للآية (تفسير · استماع · علامة · مشاركة · المزيد) + شريط تلاوة مصغّر
   بتظليل الكلمة). **خيارات الآية** (`56:50`): ورقة بنص الآية بخطّ Amiri Quran وثماني بلاطات وثلاث
   شرائح. **التنقّل والبحث** (`56:52`): حقل بحث، آخر المواضع، مقسّم رباعي، شبكة الأجزاء ٦×٥ بأوائلها
   والجزء الحالي معبّأ، وبطاقة «الانتقال بالخطّ الذهبي» بمقبض وأثلاث. **التلاوة الآن** (`56:54`):
   ميدالية القارئ، صندوق الآية كلمةً بكلمة، موجة وأزمنة، تحكّم، وشرائح (القارئ · تنزيل السورة ·
   متابعة الصفحة). **العرض والألوان** (`56:56`): معاينة حيّة، ثلاث مجموعات سمات (ورق · أخضر · داكن)
   بحرف «ق»، تعتيم، إبقاء الشاشة، طريقة العرض، التجويد، اتجاه التصفح. **الختمة والورد** (`56:58`):
   بطاقة زمرّدية بحلقة ونسبة كبيرة وموعد الإتمام و«على الجدول»، ثلاثة إحصاءات، مدة وبداية وتذكير،
   والتحدّيات بتقدّم. **مراجعة الحفظ** (`56:60`): حجب الكلمات بأشرطة لا ضباب، حبّة حالة علوية، ولوحة
   بميكروفون بهالة نعناعية وتقدّم وإجراءات الكشف. **الدليل** (`56:62`): الفكرة، اللوحة (ورق الصفحة
   `#F7F2E4`، حبره `#2A2521`، ذهبه `#A98A3A`، الزمرّد `#0E5C55`، الذهب `#C69C3E`…)، الطباعة (Reem Kufi
   للعناوين، Amiri Quran لنصّ الآية في الواجهة، Amiri لأسماء السور، Readex Pro للواجهة)، مناطق اللمس
   في القارئ، ما تغيّر عن التطبيق، والحركة والمقاسات. **لم يُنفَّذ بعد في التطبيق** — بانتظار اعتماد المالك.

خلفيّات الشاشات هي صورة صفحة ٢٧٠ الحقيقيّة المرسومة بخطّ QCF v1 (`tools/` في `sakinah/`).

## المتغيّرات

`سكينة/ألوان القارئ` — وضعان: **كريمي** و**داكن**، مشتقّان من `MUSHAF_THEMES` في
`sakinah/js/core/mushaf-themes.js` (`cream` و`dark`). كل متغيّر محدود النطاق (scopes) عمدًا،
فلا يظهر لون نصّ في خانة تعبئة إطار.

| المتغيّر | كريمي | داكن | مصدره في الشيفرة |
| --- | --- | --- | --- |
| `ورق/الصفحة` | `#f6f1e2` | `#171916` | `theme.paper` |
| `ورق/الحافة` | `#efe7d1` | `#21241f` | `theme.paper2` |
| `ورق/سطح الشريط` | `#faf6e9` | `#1e211c` | جديد — سطح الشريط الطافي |
| `حبر/أساسي` | `#1d1a14` | `#ece5d2` | `theme.ink` |
| `حبر/ثانوي` | `#747066` | `#979387` | `ink` بشفافيّة ٦٠٪ فوق الورق |
| `حبر/خط فاصل` | `#e0dccd` | `#2c2d29` | `ink` ١٠٪ |
| `حبر/تعبئة رقيقة` | `#ebe6d8` | `#22231f` | `ink` ٥٪ |
| `حبر/حد رقيق` | `#dcd7c9` | `#31312d` | `ink` ١٢٪ |
| `ذهبي/أساسي` | `#a98a3a` | `#b8993f` | `--gold-1` |
| `ذهبي/مسار` | `#e9dcb2` | `#4a3f22` | `--gold-3` |
| `علامة/أساسي` | `#0f766e` | `#2dd4bf` | لون العلامة في الويب |
| `علامة/على العلامة` | `#f6f1e2` | `#0c1514` | نصّ فوق لون العلامة |

`سكينة/مقاييس الشريط` — ارتفاع الصفّ ٥٢، مساحة اللمس ٣٦، شريط التقدّم ٣، الحشوات والزوايا.

## الخطوط

Readex Pro (الواجهة والأرقام) وReem Kufi (العناوين) — كلاهما متوفّر في Figma بالأوزان المستعملة،
ومطابق لما يسجّله `DS.registerFonts()` في `sakinah-native/App/Sakinah/DesignSystem.swift`.

## بنية الشريط

```
الشريط (٣٩٣ × ١١٤ = ٥٩ حشوة أمان + ٤٦ صفّ + ٩ شريحة التقدّم)
└── الصفّ (أفقي، حشوة جانبيّة ١٠، تباعد ٨، توسيط عمودي)
    ├── مجموعة الإجراءات (كبسولة ١٣٦×٤٠) ← أقصى اليسار
    │   ├── زر/المزيد (٣٢)      → قائمة الإجراءات الخمسة
    │   ├── زر/الفهرس (٣٢)      → السور والأجزاء
    │   ├── زر/الحفظ (٣٢)       → يمتلئ بلون العلامة أثناء الجلسة
    │   └── زر/تشغيل (٣٢، دائرة ممتلئة بلون العلامة)
    ├── كتلة العنوان (تتمدّد) ← الوسط، محاذاة لليمين
    │   ├── الاسم: ⌄ + «سورة النحل» + علامة ذهبيّة إن كانت الصفحة معلَّمة
    │   └── التفاصيل: «صفحة ٢٧٠ · الجزء ١٤ · الحزب ٢٧ · مكية»
    └── زر/إغلاق (٣٦) ← أقصى اليمين
└── شريحة التقدّم (٩) — خطٌّ ذهبي بثلاث نقاط في قاعها وستٌّ فوقه مساحةَ لمس
```

خلفيّة الشريط هي **ورق الصفحة نفسه** لا لونًا مرفوعًا عنه: السمات ستّ عشرة، ولا يوجد مقدارُ رفعٍ
واحد يصلح لها كلّها (سمة «أبيض» ورقها ‎#fbfaf6‎ فلا يبقى مجال للرفع). الفصل عن الصفحة يقع على
الظلّ (حبر بشفافيّة ١٠٪، إزاحة ٣، نصف قطر ١٠) وعلى الخطّ الذهبي، وكلاهما يعمل في السمات كلّها.

## السحب على الخطّ الذهبي

الخطّ نفسه هو منزلق الـ٦٠٤ صفحة. ثلاث قرارات تنفيذيّة تستحقّ التسجيل:

1. **مساحة اللمس داخل حدود الشريط.** الشريحة تسع نقاط لا ثلاث، ولم أمدّها إلى الصفحة تحت الشريط
   لأن SwiftUI لا يضمن وصول اللمس إلى ما رُسم خارج حدود أبيه. تسع نقاط دون الحدّ الموصى به،
   ولذلك بقي الانتقال متاحًا من ثلاثة أبواب أخرى: نقر كتلة العنوان (الفهرس)، و«بحث وتنقّل»،
   وإجراء VoiceOver القابل للضبط على الخطّ نفسه.
2. **يُرسم ويُقاس في فضاء من اليسار إلى اليمين صراحةً** (`layoutDirection = .leftToRight` على
   iOS و`LocalLayoutDirection provides Ltr` على أندرويد). لولا ذلك لالتبس أصل الإحداثيّات
   باتّجاه الكتابة، ولجرى المنزلق عكس ما يتوقّعه الإصبع.
3. **الصفحة الجارية تحت الإصبع تُحفظ في حالة لا في وسيط.** كتلة اللمس في Compose تُنشأ مرّة
   واحدة (`pointerInput(Unit)`)، فلو قرأ `onDragEnd` وسيط الصفحة لعاد إلى قيمته الأولى.

## القوائم والإجراءات

لم يسقط شيء مع الشريط السفلي. زرّ «المزيد» قائمةٌ أصليّة (`Menu` على iOS و`DropdownMenu` على
أندرويد) فيها: علامة الصفحة، بحث وتنقّل، العرض والخطّ والسمة، إخفاء الآيات للحفظ، التلاوات دون
اتّصال، ثم فاصل وبند سادس يفتح نافذة «خيارات المصحف» الكاملة (الختمة، متابعة التلاوة، تظليل
الكلمة…). القائمة اختصار لا مخبأ.

الإخفاء التلقائي للشريط بعد ٣٫٦ ثانية يتوقّف ما دامت القائمة مفتوحة، ويُعاد جدولته مع أوّل
اختيار — القائمة ليست نافذة فلا يراها الحارس الذي يراقب النوافذ.

كتلة العنوان كلّها قابلة للنقر وتفتح الفهرس أيضًا، وللفهرس زرّ ظاهر في الكبسولة لأنّه كثير
الاستعمال. الذي خرج إلى قائمة «المزيد»: علامة الصفحة، بحث وتنقّل، العرض والخطّ والسمة،
إخفاء الآيات للحفظ، التلاوات دون اتّصال.

## مقاس العنوان — القياس والحدّ الأقصى

على شاشة ٣٩٣ نقطة، بعد الكبسولة (١٣٦) وزرّ الإغلاق (٣٦) والحشوات والتباعدات (٣٦)،
يتبقّى لكتلة العنوان **١٨٥ نقطة**. قيست بخطّ Readex Pro نفسه داخل Figma:

| المحتوى | العرض | الحكم |
| --- | --- | --- |
| «سورة النحل» + السهم + العلامة | ١١٩ | يتّسع |
| «سورة المنافقون» (أطول اسم) + السهم + العلامة | ١٥٠ | يتّسع |
| «صفحة ٢٧٠ · الجزء ١٤ · الحزب ٢٧ · مكية» | ١٨٣ | يتّسع |
| «صفحة ٥٥٣ · الجزء ٢٨ · الحزب ٥٥ · مدنية» (الأطول) | ١٩٧ | **يتجاوز ١٢ نقطة** |

اسم السورة يتّسع دائمًا. سطر التفاصيل وحده يتجاوز في أطول تركيبة، فيصغر إلى ٠٫٩٢ من حجمه
(١١ → ١٠٫١) ليبقى سطرًا واحدًا — لا قصّ ولا التفاف:

- iOS: `.lineLimit(1).minimumScaleFactor(0.92)` على سطر التفاصيل فقط.
- أندرويد: `AutoShrinkText` — إصدار Compose المثبّت هنا (BOM 2024.09.03) أقدم من `autoSize`
  في `BasicText`، فالتصغير يقع بقياس الفيض في `onTextLayout` خطوةً خطوة حتى ٠٫٩٢.

اسم السورة يبقى بلا تصغير؛ إن ضاق يومًا فالخطأ في الأزرار لا في الاسم.
